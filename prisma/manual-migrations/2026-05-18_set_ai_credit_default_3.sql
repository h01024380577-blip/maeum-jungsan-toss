-- =====================================================================
-- 신규 사용자 AI 분석 웰컴 크레딧 기본값 조정 (2026-05-18)
-- =====================================================================
-- 목적:
--   신규 가입/게스트 사용자의 AI 분석 기본 잔고를 5회에서 3회로 낮춘다.
--   AI_CREDIT cap(5)보다 낮게 시작해야 사용자가 즉시 리워드 광고를 볼 수 있다.
--
-- 주의:
--   기존 사용자의 aiCredits 잔고는 변경하지 않는다.
--   이미 광고로 충전했거나 남은 잔고가 있는 사용자를 일괄 차감하지 않기 위함이다.
-- =====================================================================

BEGIN;

ALTER TABLE "User"
  ALTER COLUMN "aiCredits" SET DEFAULT 3;

COMMIT;
